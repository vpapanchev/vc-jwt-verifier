---
services:
  vc_jwt_verifier:
    server:
      host:
      port:
      debug:
    resolver:
      host: uni-resolver
      port: 8080
      api:  "/1.0/identifiers/{did}"
    multiprocessing:
      num_processes: 2
    logging:
      level: DEBUG
